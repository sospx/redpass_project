version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: password_db_container
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: password_db
    ports:
      - "5432:5432"